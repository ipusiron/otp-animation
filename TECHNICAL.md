# 技術的詳細・制限事項

## UTF-8とバイトの中核

js/otp-core.jsのOtpCoreが文字列、バイト列、ビット列、16進数の変換とXORを担当します。
DOMに依存せず、ブラウザーではclassic script、Node.jsではCommonJSとして同じ実装を読み込みます。
file://でも動かすため、ES moduleやfetchによる辞書読み込みは使いません。

平文はTextEncoderでUTF-8に変換し、64バイトを上限とします。
空文字列、制御文字U+0000〜U+001FとU+007F、孤立サロゲートを拒否します。
TextDecoderで読めない並びは、置換文字U+FFFDと注記を表示します。
鍵はcrypto.getRandomValuesで作り、バイト数を平文または暗号文に合わせます。

16進数は大文字2桁を空白で区切ります。
入力は0x、大文字と小文字、空白、コロン、ハイフン、アンダースコアに対応します。
ビット列は空白、アンダースコア、ハイフン、カンマ、縦棒を区切りとして扱い、8の倍数でない列は拒否します。
既知解答とREADMEの表はOtpCoreから導き、6件の参照値と200本の往復テストで確かめます。

## 暗号化・復号処理のアニメーション制御

状態はバイト配列と済んだビット数です。
暗号化では平文と鍵から暗号文を、復号では暗号文と鍵から平文を必要時に導きます。
結果の別配列を更新し続ける方式ではありません。
入力の変更時にタイマーを止め、古い鍵または結果と進捗を無効にします。

- 速度は0.1〜2秒の5段階
- 1ビット単位の前進と後退、リセット、全処理
- 平文のUTF-8文字単位でバイトをまとめ、8ビットごとの16進数を表示
- 使用済み鍵は燃える演出の後に灰色表示。安全なメモリー消去ではない

## OTP実験室

実験2〜4・6の文字入力はASCIIの32〜126、実験5はUTF-8で64バイトまでです。
内部のXORには暗号化タブと同じバイト中核を使います。

### 実験1のXORゲート構成

A⊕B＝(A∧¬B)∨(¬A∧B)をNOT、AND、ORで構成します。
入力を変えるとSVGと表の値が変わり、表のセルにマウスを重ねると対応する回路ラベルが強調されます。
配線色はsignal-fill、signal-stroke、signal-activeなどのCSSクラスで切り替えます。

### 実験2の鍵再利用

同じ鍵で2つの平文を暗号化し、C₁⊕C₂とP₁⊕P₂の一致を表示します。
長さが違う場合は短い方にそろえ、切り詰めた文字数を注記します。

### 実験3の断片解析

既知の平文断片と1から始まる位置から、対応する鍵の断片を導きます。
位置の空欄、整数以外、範囲外を拒否します。
既知の断片だけで未知の全平文が得られるわけではありません。

### 実験4のクリブ・ドラッグ

OtpCoreのCRIB_SAMPLEには、MEET ME AT THE NORTH GATE AT NOONとTHE PACKAGE IS UNDER THE OLD OAK.を置きます。
どちらも33バイトで、ボタンを押したときに同じ新しい鍵で暗号化します。開閉では鍵を生成しません。
自分の2文は各64文字までで、長さが違えば短い方にそろえます。

- isReadable(bytes)：空ではなく、英字・空白・ピリオド・カンマ・アポストロフィーだけかを判定
- cribDrag(x, crib)：XOR列のすべての位置でクリブを当て、offset・bytes・text・readableを返却
- assemble(x, placements)：配置した語からp1・p2・known・conflictを導出。未確定は_、食い違いは昇順の位置

cribStateはXOR列、配置履歴、候補、検索時のクリブ、題材かどうか、エラーを保持します。
表示位置は1から、中核のoffsetは0から数えます。候補を置く操作では検索したときのクリブを使います。
「 THE 」の全29位置のうち、読める候補は位置5の「PXN. 」、11の「E IS 」、21の「 GATE」です。
位置5は偶然の外れです。位置11で平文1、位置21で平文2に置くと、題材の断片と一致します。
取り消しは履歴の末尾を外して再計算します。全体の33文字と両方の題材が一致したときだけ完成です。

### 実験5の完全秘匿性

forgeKey(cipher, alternate)は暗号文と別の平文のUTF-8列をXORし、その平文に復号される鍵を返します。
長さが違う場合の中核の例外はlengthMismatchです。画面では暗号文と入力のバイト数を添えます。
secrecyStateは元の暗号文と鍵、導いた鍵、復号結果、翻訳可能なエラーと補間値を保持します。
元の平文の編集は暗号文と両方の鍵を消去し、別の平文の編集は同じ暗号文から鍵を導き直します。
ATTACK AT DAWN・RETREAT AT SIX・撤退せよ!!はいずれも14バイトです。
本物の鍵と別の平文用の鍵の参照値はREADMEの表と中核テストで確認します。

### 実験6の改ざん

flip(cipher, offset, known, target)は指定位置の暗号文をknown⊕targetで反転し、元の配列を変更せずに返します。
この計算には鍵を渡しません。断片の長さ違いはlengthMismatch、範囲外はoutOfRangeです。
tamperStateは元の暗号文と鍵、変更後の暗号文、受信者の復号結果、エラーを保持します。
画面では位置から1を引いて渡し、差分は元の暗号文と変更後の暗号文のXORから導きます。
平文の編集は鍵を含む前の結果を消去し、攻撃者の条件の編集は変更後の結果だけを消去します。
PAY 100 YEN TO BOBの位置5で100→900にすると、差分は5バイト目だけ08、復号結果はPAY 900 YEN TO BOBです。
位置16でBOB→EVEならPAY 100 YEN TO EVEになります。鍵は既定でdetailsの中に伏せ、受信者の復号だけに使います。
完全秘匿性と改ざん検知は別です。メッセージ認証コードやAES-GCMなどで完全性を保護します。

実験4〜6の描画はtextContent・createElement・replaceChildrenを使います。
言語切り替えは保存済みの状態を再描画し、配置・暗号文・変更後の結果を保持します。
実験の値はOtpCoreから導き、乱数鍵や平文を保存・外部送信しません。

## ダークモードと言語

headでtheme-init.jsを同期実行し、最初の描画前にhtmlへテーマクラスを付けます。
保存がない場合はOSの配色を使います。
言語は?lang=ja|en、保存値、navigator.languageの順で決めます。
辞書はjs/i18n.jsにまとめ、data-i18nとDOM APIで画面へ適用します。
切り替えで平文、鍵、済んだビット数、実験結果を保ちます。

- otp-animation-dark-mode：lightまたはdark
- otp-language：jaまたはen
- 平文、鍵、暗号文はlocalStorageへ保存しない
- localStorageを読めない場合も操作を継続可能

## クリップボードAPI制限とフォールバック戦略

クリップボードアクセスにはブラウザーの権限やユーザー操作が必要です。
読み取りはPromise.raceで2秒のタイムアウトを設けます。
失敗時はブロッキングダイアログを開かず、「入力欄に直接貼り付けてください」と通知します。
コピーはClipboard APIを使い、失敗した場合だけ従来のコピー手段を試します。

貼り付けは先にビット列として読み、次に16進数として読みます。
0と1と区切りだけの不完全なビット列を、16進数として誤って受理しません。
平文の貼り付けはUTF-8の妥当性と64バイト上限も確認します。

## 対応ビット列形式

```text
01001000_01000101_01001100_01001100_01001111
01001000 01000101 01001100 01001100 01001111
0100100001000101010011000100110001001111
01001000-01000101-01001100-01001100-01001111
01001000,01000101,01001100,01001100,01001111
```

## CSPとアクセシビリティ

meta CSPはscript-srcとstyle-srcをselfに限定します。
インラインハンドラー、style属性、JavaScriptからのstyle代入は使いません。
metaで無効なframe-ancestorsは指定しません。
外部要求と秘密情報のconsole出力はありません。
書き出すファイルには鍵も含まれるため、教育用の記録として扱います。

タブにはARIAと左右キー操作、ヘルプにはTab循環とEscによるフォーカス復帰を用意しています。
?は入力中には奪わず、Ctrl/Cmd+Dを変更しません。
狭い画面ではビットと表の箱内で横スクロールします。
操作要素の高さは44px以上、文字色と背景色はライトとダークの両方で4.5:1以上を検査します。
動きを減らす設定ではCSSの演出を抑えます。

## テスト

Node.js 22のnpm testで6本のテストファイルを実行します。
中核、辞書、HTMLと禁止API、配色、整形、READMEの表と構造を検査します。
pushとpull_requestでも同じコマンドをGitHub Actionsで実行します。
UIの検証はHTTPとfile://の両方で、日英、ライトとダーク、幅1280・768・390・320pxを確認します。
